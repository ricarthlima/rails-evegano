<%= form_with(model: componente_nome_alternativo, local: true) do |form| %>
  <% if componente_nome_alternativo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(componente_nome_alternativo.errors.count, "error") %> prohibited this componente_nome_alternativo from being saved:</h2>

      <ul>
      <% componente_nome_alternativo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :componente_id %>
    <%= form.text_field :componente_id %>
  </div>

  <div class="field">
    <%= form.label :nome_alternativo %>
    <%= form.text_field :nome_alternativo %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
