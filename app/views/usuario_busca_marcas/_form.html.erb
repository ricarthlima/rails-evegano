<%= form_with(model: usuario_busca_marca, local: true) do |form| %>
  <% if usuario_busca_marca.errors.any? %>
    <div id="error_explanation">
      <h2 class = "color-font"><%= pluralize(usuario_busca_marca.errors.count, "error") %> prohibited this usuario_busca_marca from being saved:</h2>

      <ul>
      <% usuario_busca_marca.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :usuario_id %>
    <%= form.text_field :usuario_id %>
  </div>

  <div class="field">
    <%= form.label :marca_id %>
    <%= form.text_field :marca_id %>
  </div>

  <div class="field">
    <%= form.label :dt_busca %>
    <%= form.text_field :dt_busca %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
