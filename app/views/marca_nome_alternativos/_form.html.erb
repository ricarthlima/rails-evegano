<%= form_with(model: marca_nome_alternativo, local: true) do |form| %>
  <% if marca_nome_alternativo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(marca_nome_alternativo.errors.count, "error") %> prohibited this marca_nome_alternativo from being saved:</h2>

      <ul>
      <% marca_nome_alternativo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :marca_id %>
    <%= form.text_field :marca_id %>
  </div>

  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :nome %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
